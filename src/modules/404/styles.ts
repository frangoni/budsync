import styled from 'styled-components';

export const StyledBud = styled.img`
	width: 100%;
	max-width: 20rem;
	filter: drop-shadow(6px 6px 6px black);
	-webkit-filter: drop-shadow(6px 6px 6px black);
`;
